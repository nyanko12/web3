# StudyFlow 仕様書

自動スケジューリング型の個人用学習スケジューラー

---

## 1. 概要

### 1.1 目的

ユーザーが「勉強したい内容」と「目標期間」、そして「勉強できない時間」を入力するだけで、最適な勉強スケジュールを自動生成するアプリ。スケジュールを手動で組む手間を省き、目標達成までの計画作成を支援する。

### 1.2 ターゲットユーザー

個人ユーザー（学生、社会人、資格試験受験者など）

### 1.3 プラットフォーム

- Webブラウザ（HTML/CSS/JavaScript単体で動作）
- 将来的にはデスクトップアプリ化（Electron等）も視野

### 1.4 設計方針

- **シンプル・使いやすさ重視**：3ステップで完結
- **オフライン動作**：外部APIに依存せず、ブラウザ単体で完結
- **ローカル動作**：ユーザーのデータは外部送信しない

---

## 2. 機能要件

### 2.1 機能一覧

| 優先度 | 機能 | 概要 |
|--------|------|------|
| 必須 | 科目登録 | 名前・期間・1日の目標時間・優先度を登録 |
| 必須 | ブロック時間設定 | 勉強できない曜日・時間帯を指定 |
| 必須 | スケジュール自動生成 | 入力情報からカレンダーを自動構築 |
| 必須 | カレンダー表示 | 週単位で生成結果を表示、前後の週に移動可 |
| 必須 | 統計表示 | 合計時間・週平均・セッション数を表示 |
| あると便利 | 複数科目対応 | 複数の目標を同時にスケジューリング |
| あると便利 | 警告通知 | 目標達成困難な場合のアラート |
| 将来追加 | データ永続化 | LocalStorageで入力内容を保存 |
| 将来追加 | エクスポート | iCal/CSV形式での書き出し |
| 将来追加 | 進捗トラッキング | セッション完了チェック機能 |

### 2.2 ユーザーフロー

```
[Step 1] 目標を設定
   ↓ 科目を1つ以上追加
[Step 2] 勉強できない時間を設定
   ↓ 曜日と時間帯を指定
[Step 3] スケジュール生成・表示
   ↓ 必要に応じてStep 2に戻り再生成
```

---

## 3. 入力項目仕様

### 3.1 科目の入力項目

| 項目 | 型 | 必須 | デフォルト | 説明 |
|------|-----|:----:|-----------|------|
| 科目名 | 文字列 | ○ | - | 表示用の名前（例：数学II） |
| 優先度 | 列挙(高/中/低) | ○ | 中 | スケジューリング時の優先度 |
| 1日の目標時間 | 数値（分） | ○ | 60 | 15〜480分の範囲 |
| 1セッションの長さ | 列挙 | ○ | 60分 | 30/45/60/90/120分から選択 |
| 開始日 | 日付 | ○ | 今日 | スケジュール開始日 |
| 終了日 | 日付 | ○ | 30日後 | 締め切り日 |
| 具体的な目標 | 文字列 | × | - | メモ用（例：模試で70点） |

### 3.2 ブロック時間の入力項目

| 項目 | 型 | 必須 | 説明 |
|------|-----|:----:|------|
| ブロック曜日 | 曜日リスト | × | 完全に勉強できない曜日（日〜土） |
| ブロック時間帯 | 時刻範囲リスト | × | 毎日繰り返しブロックする時間（睡眠・学校など） |

---

## 4. スケジューリングアルゴリズム

### 4.1 アルゴリズムの概要

ルールベースの貪欲法（greedy algorithm）で実装する。

### 4.2 処理フロー

```
1. 全科目の最早開始日〜最終締め切り日を範囲とする
2. 範囲内の各日について以下を実行：
   a. 曜日がブロックされていればスキップ
   b. 24時間（0:00〜24:00）から、ブロック時間帯を差し引いて空き時間プールを作成
   c. その日に有効な科目を抽出（開始日 ≤ 当日 ≤ 終了日）
   d. 科目を優先度の高い順にソート（同優先度なら締め切りが近い順）
   e. 各科目について：
      - 1日の目標時間 ÷ 1セッションの長さ = 必要セッション数
      - 空き時間プールから1セッション分を確保
      - セッション後に15分の休憩バッファを設定
      - 目標時間に達するまで繰り返し
3. 全セッションを週単位でグループ化
4. 各科目の完了率を計算し、85%未満なら警告を生成
```

### 4.3 優先順位ルール

1. **優先度（高 > 中 > 低）**
2. **同優先度の場合は締め切りの近い順**

### 4.4 セッション配置ルール

- 1セッションは最低30分以上を確保
- セッション間は15分以上の休憩を空ける
- 空き時間内に収まらない場合は、その日の残時間に合わせて短縮（最低30分）

---

## 5. データ構造

### 5.1 Subject（科目）

```javascript
{
  id: 数値,               // ユニークID
  name: 文字列,           // 科目名
  daily: 数値,            // 1日の目標時間（分）
  session: 数値,          // 1セッションの長さ（分）
  priority: 1 | 2 | 3,    // 優先度（1=低、2=中、3=高）
  start: "YYYY-MM-DD",    // 開始日
  end: "YYYY-MM-DD",      // 終了日
  goal: 文字列,           // 具体的な目標（任意）
  color: 文字列           // 表示色（HEX）
}
```

### 5.2 Session（スケジュールされた1コマ）

```javascript
{
  date: "YYYY-MM-DD",     // 実施日
  subjectId: 数値,        // 科目ID
  subjectName: 文字列,    // 科目名
  color: 文字列,          // 表示色
  startMin: 数値,         // 開始（0:00からの分）
  endMin: 数値,           // 終了（0:00からの分）
  startTime: "HH:MM",     // 開始時刻表示用
  endTime: "HH:MM"        // 終了時刻表示用
}
```

### 5.3 BlockedSlot（ブロック時間帯）

```javascript
{
  start: 数値,            // 開始時刻（0:00からの分）
  end: 数値               // 終了時刻（0:00からの分）
}
```

### 5.4 Schedule（生成結果）

```javascript
{
  weeks: [                // 週ごとのデータ配列
    {
      weekStart: "YYYY-MM-DD",  // 週の開始日（月曜）
      sessions: [Session]       // その週のセッション一覧
    }
  ],
  advice: [文字列],       // アドバイス・警告メッセージ
  totalMin: 数値,         // 合計時間（分）
  weeklyAvg: 数値,        // 週平均（時間）
  rangeStart: 文字列,     // 全体の開始日
  rangeEnd: 文字列,       // 全体の終了日
  sessionCount: 数値      // 総セッション数
}
```

---

## 6. UI仕様

### 6.1 画面構成

3ステップ式のシングルページ。タブ式のピル（pill）ナビゲーションでステップ間を移動できる。

### 6.2 各画面の構成

#### Step 1: 目標設定画面
- 登録済み科目の一覧表示
- 新規科目入力フォーム（カード形式）
- 「次へ」ボタン

#### Step 2: ブロック時間設定画面
- 曜日選択（円形チップ7個、クリックで切替）
- 時間帯リスト（追加・削除可能、デフォルトで睡眠・学校・就寝前の3つ）
- 「戻る」「生成する」ボタン

#### Step 3: 結果表示画面
- 期間情報・週ナビゲーション（前後の週へ移動）
- カレンダーグリッド（縦軸：時刻、横軸：曜日）
- 凡例（科目別の色、ブロック時間）
- 統計バー（週数・セッション数・合計時間・週平均・科目数）
- アドバイス・警告メッセージ

### 6.3 デザイン方針

- **テイスト**：手書きノート風、紙の質感
- **タイポグラフィ**：見出しは serif（DM Serif Display）、本文は Noto Sans JP
- **配色**：オフホワイトの背景、ダークインクの文字、アクセントは深いオレンジ（#c2581f）と深緑（#2d6a4f）
- **質感**：ルーズリーフ風の罫線パターンを背景に配置

---

## 7. 技術仕様

### 7.1 技術スタック

- HTML5
- CSS3（CSS変数、Grid、Flexbox）
- Vanilla JavaScript（フレームワーク不使用）
- Google Fonts（DM Serif Display、DM Mono、Noto Sans JP）

### 7.2 ブラウザサポート

モダンブラウザ全般（Chrome、Firefox、Safari、Edge の最新版）

### 7.3 レスポンシブ対応

- 幅600px以下でモバイルレイアウトに切替
- フォームは2カラム→1カラム
- カレンダーの時刻列を圧縮、フォントサイズを縮小

### 7.4 ファイル構成

```
study-scheduler.html  // 単一ファイルで完結（HTML/CSS/JSを内包）
```

---

## 8. バリデーション

### 8.1 入力検証ルール

| 項目 | ルール | エラーメッセージ |
|------|--------|------------------|
| 科目名 | 空文字不可 | 「科目名を入力してください」 |
| 日付 | 必須 | 「日付を入力してください」 |
| 日付関係 | 開始日 ≤ 終了日 | 「開始日は終了日より前にしてください」 |
| 1日の目標時間 | 15〜480分 | （HTMLのmin/max属性で制御） |

### 8.2 エラーハンドリング

- 科目未登録のままスケジュール生成しようとした場合：Step 1に戻し、フォーム入力中なら自動で追加を試みる
- 時間枠が足りず目標未達の場合：警告を結果画面に表示

---

## 9. 将来の拡張案

### 9.1 データ永続化

- `localStorage` を使用して、科目とブロック時間の設定を自動保存
- 再訪時に前回の入力を復元

### 9.2 高度なスケジューリング

- 朝型・夜型の好みを反映
- 科目ごとに最適な時間帯を学習（記憶系は朝、演習系は夕方など）
- 試験日からの逆算スケジューリング

### 9.3 進捗管理

- 各セッションに完了チェックボックス
- 達成率の可視化
- 未消化分の自動再スケジュール

### 9.4 エクスポート

- iCal形式（`.ics`）でカレンダーアプリにインポート
- CSV形式でスプレッドシートで管理
- 印刷用レイアウト

### 9.5 外部連携

- Google Calendar 同期
- 通知（ブラウザのNotification API）

---

## 10. テストケース

### 10.1 正常系

| ケース | 入力 | 期待結果 |
|--------|------|---------|
| 単一科目 | 1科目を1ヶ月、毎日60分 | 平日・休日に1セッション/日が配置される |
| 複数科目 | 優先度違いの2科目 | 高優先度の科目が先に時間枠を確保 |
| 短期集中 | 1週間で1科目120分/日 | 7日分のセッションが連続生成 |

### 10.2 異常系

| ケース | 入力 | 期待結果 |
|--------|------|---------|
| 科目未登録 | 0科目で生成 | Step 1に戻り、アラート表示 |
| 期間逆転 | 開始日 > 終了日 | エラーメッセージ表示、追加されない |
| ブロック過多 | 全曜日ブロック | 「スケジュールが空です」と表示 |
| 時間枠不足 | 1日30分しか空きがないのに目標120分 | 達成率の警告を表示 |

---

## 11. 補足事項

- このアプリは外部APIを呼び出さず、ユーザーのブラウザ内で完結する
- ユーザーデータは現バージョンでは保存されず、ページ再読み込みでリセットされる（将来的に永続化を予定）
- 個人利用を想定しており、複数ユーザー対応・ログイン機能は対象外

---

**バージョン**：1.0
**最終更新**：2026-05-27
